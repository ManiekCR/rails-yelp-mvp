<div class="container pt-3"><%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "add a review", class: "btn btn-primary" %>
  <% end %>
    <p class="pt-2"><%= link_to "Back to the restaurant", restaurant_path(@restaurant)%></p>
  </div>
